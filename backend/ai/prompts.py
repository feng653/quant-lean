"""AI Prompt 模板 —— 用于 DeepSeek API 调用的提示词.

每个模板使用 Python 字符串的 format() 方法填充占位符。
"""

# ═══════════════════════════════════════════════════════════════════════════
# 回测分析
# ═══════════════════════════════════════════════════════════════════════════

BACKTEST_ANALYSIS_PROMPT = """你是一名资深量化研究员。请分析以下回测结果，提供专业的评估和建议。

## 实验信息
- 策略: {strategy_name}
- 实验ID: {experiment_id}
- 回测区间: {train_start} ~ {test_end}

## 核心指标
{metrics_summary}

## 请回答:
1. **整体评价** — 该策略的表现如何？适合什么市场环境？
2. **风险分析** — 最大回撤、波动率是否在可接受范围内？
3. **改进建议** — 有哪些参数或逻辑可以优化？
4. **部署建议** — 是否建议部署到模拟盘？需要注意哪些风险？

请用中文回答，简明扼要，控制在500字以内。
"""

# ═══════════════════════════════════════════════════════════════════════════
# 参数调优建议
# ═══════════════════════════════════════════════════════════════════════════

PARAM_SUGGESTION_PROMPT = """你是一名量化策略参数优化专家。请为以下策略提供参数调优建议。

## 策略信息
- 策略名称: {strategy_name}
- 策略类型: {strategy_category}
- 策略描述: {strategy_description}

## 当前参数
{current_params}

## 策略元数据中的参数定义
{param_definitions}

## 历史最佳实验指标
{best_metrics}

## 输出契约
只输出一个合法 JSON 对象，禁止 Markdown、代码围栏、解释性前后缀或自由文本。
格式必须严格为：
{{"suggestions":[{{"param_name":"参数名","current_value":当前值,
"suggested_value":建议值,"reason":"中文理由"}}]}}

param_name 只能来自参数定义；current_value 必须与当前参数（未提供时为默认值）
一致；suggested_value 必须符合参数类型、choices 和 min/max 范围。
"""

# ═══════════════════════════════════════════════════════════════════════════
# 市场解读
# ═══════════════════════════════════════════════════════════════════════════

MARKET_INSIGHT_PROMPT = """你是一名宏观量化分析师。请分析当前投资组合的市场表现和风险暴露。

## 组合信息
- 组合名称: {portfolio_name}
- 总资金: {total_capital}
- 再平衡频率: {rebalance_frequency}
- 当前持仓数: {position_count}

## 配置
{allocations}

## 近期表现
{recent_performance}

## 当前持仓行业分布
{industry_exposure}

## 请回答:
1. **市场环境** — 当前市场环境对组合是否有利？
2. **风险暴露** — 组合在行业、风格、市值上的主要风险暴露是什么？
3. **调整建议** — 是否有需要调仓的地方？
4. **展望** — 未来1-3个月的关注重点。

用中文回答，控制800字以内。
"""

# ═══════════════════════════════════════════════════════════════════════════
# 错误诊断
# ═══════════════════════════════════════════════════════════════════════════

ERROR_DIAGNOSIS_PROMPT = """你是一名量化系统技术专家。请诊断以下实验执行中的错误并提供解决方案。

## 实验信息
- 策略: {strategy_name}
- 实验ID: {experiment_id}
- 状态: {status}

## 错误日志
```
{error_log}
```

## 实验配置
- 股票池: {pool_preset} {pool_custom_codes}
- 训练区间: {train_start} ~ {train_end}
- 测试区间: {test_start} ~ {test_end}
- 参数: {params}

## 输出契约
只输出一个合法 JSON 对象，禁止 Markdown、代码围栏或任何前后缀：
{{"category":"strategy_interface|strategy_code|data|params|environment|unknown",
"root_cause":"根因摘要","evidence":"直接证据","fix_suggestion":"修复建议",
"auto_fixable":false}}

category 必须取上述白名单之一；三个文本字段必须为非空中文字符串；
auto_fixable 必须是 JSON 布尔值，且只有 strategy_interface 或 strategy_code
类别允许为 true，其余类别必须为 false。不得声称已自动修改系统。
"""

# ═══════════════════════════════════════════════════════════════════════════
# 信号解释
# ═══════════════════════════════════════════════════════════════════════════

SIGNAL_EXPLAIN_PROMPT = """你是一名量化交易策略分析师。请解释以下交易信号的产生逻辑。

## 策略信息
- 策略名称: {strategy_name}
- 策略类型: {strategy_category}
- 策略描述: {strategy_description}

## 信号详情
- 股票代码: {code}
- 信号类型: {action}
- 信号强度: {score}
- 置信度: {confidence}

## 上下文数据
{context}

## 请回答:
1. **信号逻辑** — 该策略为什么会在这个时间点对这个股票产生这个信号？
2. **可靠性评估** — 该信号的可靠性如何？
3. **风险提示** — 执行该信号需要注意的风险。

用中文回答，简洁明了。
"""
