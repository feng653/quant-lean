# 实际工作唯一索引（版本队列）

> 生效日期：2026-08-03 ｜ 与 GitHub issue 一一对应。issue 是任务真源，本文件是版本顺序的只读镜像。
> 状态只使用：`待开始`、`进行中`、`待验收`、`完成`。
> **唯一进行中规则**：同一时刻只允许一个版本处于 `进行中`（其子任务可并行）；给下一个版本加 `agent` 标签前，当前版本必须已合入并验收。

## 代码版本队列

| 顺序 | 版本 | 单一方向 | GitHub Issue | 状态 |
|---:|---|---|---|---|
| 1 | v0.3.0 | 契约锁定层（快照基建 + CI 门禁） | #1 | 待开始 |
| 2 | v0.4.0 | main.py 抽层（engine/ + jobs/handlers/） | #2 | 待开始 |
| 3 | v0.5.0 | 去重（哈希/时间/错误/分页/提交 → 各 1 份） | #3 | 待开始 |
| 4 | v0.6.0 | 删除未用功能（80 端点、远程训练、晋级流、死代码） | #4 | 待开始 |
| 5 | v0.7.0 | 数据收敛（5 套价格→1、4 套复权→1、删双账本） | #5 | 待开始 |
| 6 | v0.8.0 | 行为简化（删 QA/能力令牌/Ed25519、14 权限→2、门禁放宽） | #6 | 待开始 |

## 无代码操作队列

- 数据迁移（31G 运行时数据从冻结旧仓库迁入 data/）——已由人工在仓库建立时执行（后台 rsync）。
- 真实数据验收（每版本手工 E2E：启动后端 → 调受影响 API → 前端核对）——每版本 `待验收` 时执行。
- GitHub 配置（self-hosted runner、API key secret、Merge Queue）——基础设施项，见 `docs/WORKFLOW_AUTOMATION.md`。

## 完成定义

代码项只有在：实现 → 测试 → PR 合入 → 版本验收（手工 E2E）→ 打 tag 后，才可标记 `完成`。
仅"代码存在"或"测试通过"不算完成。
