# 实际工作唯一索引（版本队列）

> 生效日期：2026-08-03 ｜ 与 GitHub issue 一一对应。issue 是任务真源，本文件是版本顺序的只读镜像。
> 状态只使用：`待开始`、`进行中`、`待验收`、`完成`。
> **并行规则**：同一 domain 标签同一时刻只允许一个任务开工（守卫强制）；不同 domain 可并行。

## 代码版本队列

| 顺序 | 版本 | 单一方向 | GitHub Issue | Domain | 状态 |
|---:|---|---|---|---|---|
| 1 | v0.3.0 | 契约锁定层（快照基建 + CI 门禁） | #1 | infra, tests | ✅ 完成 |
| 2 | v0.4.0 | main.py 抽层（engine/ + jobs/handlers/） | #2 | engine, jobs | ✅ 完成 |
| 3 | v0.5.0 | 去重（哈希/时间收敛；错误类/分页评估记录） | #3 | core | ✅ 完成 |
| 4 | v0.6.0 | 死代码删除（cache.py 337 行） | #4 | api, frontend | ✅ 完成 |
| 5 | v0.6.1 | 端点删除低风险子集（逐端点取证） | #19 | api, frontend | 待开始 |
| 6 | v0.7.1 | 数据层盘点与统一存储设计 | #20 | data | 待开始 |
| 7 | v0.7.2 | 数据对账基建（新旧逐日比对） | #21 | data | 待开始 |
| 8 | v0.7.3 | 复权口径统一（4 套→1 套） | #22 | data | 待开始 |
| 9 | v0.7.4 | 双账本移除（price_ledger 退役） | #23 | data | 待开始 |
| 10 | v0.8.1 | QA 认证死路拆除（告警降级） | #24 | data | 待开始 |
| 11 | v0.8.2 | 权限体系 14→2 档 | #25 | api | 待开始 |
| 12 | v0.8.3 | 统一异常处理器（58 处 500 样板） | #26 | api | 待开始 |
| 13 | v0.8.4 | 模拟盘数据等级放宽 | #27 | api, data | 待开始 |
| 14 | v0.8.5 | 同义就绪字段收敛（5→3+1） | #28 | data | 待开始 |

## 并行组合（domain 互斥判定）

| 可并行组合 | 说明 |
|---|---|
| v0.7.1（data）+ v0.6.1（api/frontend） | 数据域与 API 域互不重叠 |
| v0.8.1（data）+ v0.8.2（api） | 同上 |
| v0.7.1（data）+ v0.8.2（api） | 同上 |

**串行约束**：data 域内（v0.7.1→v0.7.2→v0.7.3→v0.7.4→v0.8.1→v0.8.5）全串行；
api 域内（v0.8.2→v0.8.3→v0.6.1）串行。守卫自动强制。

## 无代码操作队列

- 数据迁移（31G 运行时数据从冻结旧仓库迁入 data/）——已由人工在仓库建立时执行（后台 rsync）。
- 真实数据验收（每版本手工 E2E：启动后端 → 调受影响 API → 前端核对）——每版本 `待验收` 时执行。
- GitHub 配置（self-hosted runner、API key secret、Merge Queue）——基础设施项，见 `docs/WORKFLOW_AUTOMATION.md`。

## 完成定义

代码项只有在：实现 → 测试 → PR 合入 → 版本验收（手工 E2E）→ 打 tag 后，才可标记 `完成`。
仅"代码存在"或"测试通过"不算完成。
