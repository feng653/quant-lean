# 执行待办兼容入口

> 自 2026-08-02 起，本文件不再维护任务状态或队列，以免形成第二真源。

实际工作唯一入口是 [todo/TODO_INDEX.md](todo/TODO_INDEX.md)。它分别索引：

- 每个小版本独立的代码更新 TODO（包括实现、测试、审查、合并和部署）；
- 独立的量化实验与运行操作 TODO（不修改代码）。

旧 WIP 和 Q-R 编号的迁移映射保存在该索引中。用户应直接修改 `docs/todo/` 内对应文件；
Agent 必须在每完成一项后从磁盘重读全部 TODO，直到没有未完成项目才停止。
