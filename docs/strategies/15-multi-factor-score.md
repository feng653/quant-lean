# 多因子综合评分（multi_factor_score_v1）

## 原理

分别计算短期反转、低波动、流动性和截面动量的 0–1 排名，再按配置权重加权合成。各底层因子均使用 T-1 及以前数据。

## 参数

每个因子提供 `use_*` 开关及 `*_weight` 非负权重；`top_k_pct` 控制最终入选比例。至少一个启用因子的权重必须大于零。

无需训练，适合作为透明、可扫描的多因子基线。
