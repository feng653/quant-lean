# 唐奇安通道突破（donchian_breakout_v1）

## 原理

策略维护每只股票的持仓状态：前一交易日收盘突破此前 `entry_period` 日最高价时买入，跌破此前 `exit_period` 日最低价时卖出。通道排除确认日自身，T 日信号不读取 T 日行情。

## 参数

- `entry_period`：入场通道，默认 55。
- `exit_period`：离场通道，默认 20，必须小于入场周期。

缺少高低价时以收盘价降级计算。
