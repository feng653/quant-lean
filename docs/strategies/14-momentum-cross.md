# 截面动量因子（momentum_cross_v1）

## 原理

比较跳过最近若干月后的历史价格与回看期价格，形成经典 12-1 动量，再按月进行截面排名。公式两端均至少滞后一个交易日。

## 参数

- `lookback_months`：默认 12。
- `skip_months`：默认 1，且必须小于回看月数。
- `top_k_pct`：默认 10%。

无需训练；每月首个交易日输出强势股票的 `BUY` 信号。
