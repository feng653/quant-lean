# 风险平价策略组合（composite_riskparity_v1）

## 原理

将子策略信号转换为近似纸面日收益，按过去 `lookback_days` 日波动率倒数分配策略权重。权重每月首次遇到信号时计算，且只使用该日期以前的绩效。

## 参数

- `sub_strategy_ids`：原子子策略列表。
- `lookback_days`：默认 126。

历史不足或波动率不可用时回退到等权。递归和未知子策略保护与等权组合一致。
